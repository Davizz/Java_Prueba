package C14;

public class Barcos {

	public Barcos() {
//		Crear un nuevo proyecto Barcos y su paquete correspondiente escrito en minúsculas. 
//		En el paquete crear una clase Main que será la responsable de ejecutar la aplicación. 
//		A continuación, crear una clase Barco con propiedades públicas 
//		(String) nombre, (int) ancho, (int) longitud, (byte) plazas, (double) potencia y (int) altura. 
//		Las medidas ancho, longitud y altura se tomarán todas en centímetros. La potencia en caballos. 
//		Añadirle unos métodos obtenerLongitud y obtenerAltura y pasarles los parámetros correspondientes. 
//		Crear un método obtenerDatos que contenga el nombre y la potencia del barco.  
//		Crear 3 constructores de la clase. 
//		Un primero por defecto (sin ningún parámetro). 
//		Un segundo con los parámetros ancho, longitud y altura. 
//		Un tercero con todos los parámetros. 
//		Los datos sin iniciar de los dos primeros constructores tendrán que tener valores por defecto. 
//		Incluir en el método main la instanciación de cada constructor. 
//		Imprimir en pantalla los resultados de cada instancia con el método obtenerDatos. 
//		A continuación, crear y utilizar un método que simule el hecho de que el barco tiene 
//		que pasar debajo de tres puentes que tienen las medidas siguientes: 
//		Puente 1: Alto 10 metros Ancho 10 metros. 
//		Puente 2: Alto 6 metros Ancho 6 metros. 
//		Puente 3: Alto 3 metros Ancho 3 metros. 
//		Establecer un mensaje adaptado que indique si el barco puede o no pasar por cada puente. 
		
		public String nombre;
		public int anchco;
		, (int) ancho, (int) longitud, (byte) plazas, (double) potencia y (int) altura
		
	}

}
